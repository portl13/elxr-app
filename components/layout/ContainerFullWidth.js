import { Container } from 'reactstrap'

const ContainerFullWidth = ({ children }) => {
    return (
        <Container>
            {children}
        </Container>
    );
}

export default ContainerFullWidth;
