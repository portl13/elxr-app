import React from 'react'
import ArrowLeft from '/public/img/icons/arrowLeft.svg'

function ArrowLeftIcon({ className }) {
  return <ArrowLeft className={className} />
}

export default ArrowLeftIcon
